#include <stdio.h>
#include <stdlib.h>

int main(){
    int i,v[5];
    
    printf("Digite os numeros para as 5 posicoes do vetor\n");

    for(i=0; i<5; i++){
        printf("v[%d] = ",i);
        scanf("%d",&v[i]);1
    }
    return 0;
}