Matrizes 2D — Exercício 1
Leitura e impressão de matriz
Escreva ler_matriz, que preenche uma matriz 3×4 via scanf, e imprimir_matriz, que a exibe com colunas alinhadas. O parâmetro int (*m)[COLS] é um ponteiro para uma linha de COLS elementos.
Protótipo
/* #define COLS 4 */void ler_matriz(int (*m)[COLS], int lin, int col);void imprimir_matriz(int (*m)[COLS], int lin, int col);
Trecho de referência (incompleto)
for(int i=0; ifor(int j=0; j"%d", &m[i][j]);

Matrizes 2D — Exercício 2
Soma de linhas
Implemente soma_linhas que preenche soma_lin[i] com a soma de todos os elementos da linha i da matriz N×COLS.
Protótipo
/* #define COLS 4 */
void soma_linhas(int (*m)[COLS], int n, int cols, int *soma_lin);
Trecho de referência (incompleto)
for(int i=0; i0;
    for(int j=0; j/*?*/;
}

Matrizes 2D — Exercício 3
Soma de colunas e diagonal
Implemente soma_colunas_diag para uma matriz quadrada N×N: preenche soma_col[j] com a soma da coluna j e armazena em *diag a soma da diagonal principal, tudo em uma única passagem.
Protótipo
/* #define N 4  (matriz quadrada N×N) */
void soma_colunas_diag(int (*m)[N], int n,
                        int *soma_col, int *diag);
Trecho de referência (incompleto)
for(int i=0; ifor(int j=0; jif(i==j) *diag+=/*?*/;
}

Transposição de matriz
Matrizes 2D — Exercício 4
Transposição de matriz
Implemente transpor que recebe uma matriz N×M e preenche trans (M×N) colocando cada elemento na posição correta. Teste com uma matriz 3×4.
Protótipo
/* #define N 3  #define M 4 */
void transpor(int (*orig)[M], int (*trans)[N], int n, int m);
Trecho de referência (incompleto)
for(int i=0; ifor(int j=0; j/*?*/][/*?*/]=orig[i][j];


Multiplicação de matrizes
Matrizes 2D — Exercício 5
Multiplicação de matrizes
Implemente multiplicar que calcula C = A × B, onde A é N×M e B é M×P, produzindo C N×P. Inicialize C com zeros antes dos cálculos.
Protótipo
/* #define M 3  #define P 4 */
void multiplicar(int (*a)[M], int (*b)[P], int (*c)[P],
              int n, int m, int p);
Trecho de referência (incompleto)
for(i) for(j){ c[i][j]=0;
    for(k) c[i][j]+=a[i][k]*b[k][j];
}

Busca em matriz
Matrizes 2D — Exercício 6
Busca em matriz
Implemente buscar_matriz que percorre uma matriz N×COLS e armazena em linhas e colunas as posições de cada ocorrência de alvo. Retorna o total de ocorrências.
Protótipo
/* #define COLS 4 */
int buscar_matriz(int (*m)[COLS], int n, int cols,
               int alvo, int *linhas, int *colunas);

Extração de submatriz

Matrizes 2D — Exercício 7
Extração de submatriz
Implemente extrair_sub que copia para sub uma região de altura h e largura w de orig, iniciando na posição (li, lj). Valide no main que a submatriz cabe dentro da original.
Protótipo
/* #define M 5  #define W 3 */void extrair_sub(int (*orig)[M], int n, int m,

              int (*sub)[W], int li, int lj, int h, int w);

Trecho de referência (incompleto)

for(int i=0; ifor(int j=0; j/*?*/][lj+/*?*/];

Maior soma de linha
Matrizes 2D — Exercício 8

Maior soma de linha
Implemente maior_soma_linha que percorre uma matriz N×COLS (podendo ter valores negativos), armazena em *soma_max a maior soma entre todas as linhas e retorna o índice dessa linha.

Protótipo
/* #define COLS 4 */
int maior_soma_linha(int (*m)[COLS], int n, int cols, int *soma_max);