Mat. em Vetor — Exercício 1
Impressão da matriz
Implemente imprimir_mat_v que recebe um vetor armazenando uma matriz N×M em formato linha a linha (fórmula do índice: i×M+j) e o imprime como tabela com N linhas e M colunas.
Protótipo
void imprimir_mat_v(int *v, int n, int m);
Trecho de referência (incompleto)
for(int k=0; k"%4d", v[k]);
    if((k+1)%m==0) /* quebra de linha */
}

Mat. em Vetor — Exercício 2
Soma de linhas
Implemente soma_linhas_v que preenche soma_lin[i] com a soma de todos os elementos da linha i de uma matriz N×M armazenada em vetor (linha a linha), usando apenas aritmética de índices.
Protótipo
void soma_linhas_v(int *v, int n, int m, int *soma_lin);
Trecho de referência (incompleto)
for(int i=0; i0;
    for(int j=0; j/*?*/];
}

Mat. em Vetor — Exercício 3
Soma de colunas e diagonal
Implemente soma_colunas_diag_v para uma matriz quadrada N×N em vetor (linha a linha): preenche soma_col[j] com a soma da coluna j e armazena em *diag a soma da diagonal principal, usando apenas aritmética de índices.
Protótipo
void soma_colunas_diag_v(int *v, int n,                             int *soma_col, int *diag);
Trecho de referência (incompleto)
for(int i=0; ifor(int j=0; jif(i==j) *diag+=/*?*/;
}

Mat. em Vetor — Exercício 4
Transposição
Implemente transpor_v que copia o vetor a (matriz N×M em linha a linha) para o vetor b (matriz M×N em linha a linha), colocando cada elemento na posição correta da transposta usando apenas aritmética de índices.
Protótipo
void transpor_v(int *a, int *b, int n, int m);
Trecho de referência (incompleto)
/* a: N×M  →  b: M×N */
for(int i=0; ifor(int j=0; j/* j*n+i */]=a[i*m+j];

Mat. em Vetor — Exercício 5
Multiplicação de matrizes
Implemente multiplicar_v que calcula C = A × B, com A (N×M), B (M×P) e C (N×P), todos em vetores (linha a linha). Use apenas aritmética de índices.
Protótipo
void multiplicar_v(int *a, int *b, int *c,
              int n, int m, int p);

Mat. em Vetor — Exercício 6
Busca na matriz
Implemente buscar_v que percorre uma matriz N×M em vetor (linha a linha), armazena em linhas e colunas as posições de cada ocorrência de alvo, e retorna o total de ocorrências. Use apenas aritmética de índices.
Protótipo
int buscar_v(int *v, int n, int m, int alvo,
           int *linhas, int *colunas);
Trecho de referência (incompleto)
for(int k=0; kif(v[k]==alvo){
    linhas[t]=k//*?*/;
    colunas[t]=k%/*?*/;
    t++;
  }

Mat. em Vetor — Exercício 7
Extração de submatriz
Implemente extrair_sub_v que copia para o vetor b (matriz h×w em linha a linha) a submatriz de a (N×M) iniciando em (li, lj). Use apenas aritmética de índices.
Protótipo
void extrair_sub_v(int *a, int n, int m,
              int *b, int li, int lj, int h, int w);
Trecho de referência (incompleto)
for(int i=0; ifor(int j=0; j/*?*/];

Mat. em Vetor — Exercício 8
Maior soma de linha
Implemente maior_soma_linha_v que percorre uma matriz N×M em vetor (linha a linha), armazena em *soma_max a maior soma entre todas as linhas (podendo ter valores negativos) e retorna o índice dessa linha.
Protótipo
int maior_soma_linha_v(int *v, int n, int m, int *soma_max);